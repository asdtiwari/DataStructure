/* Program to find the sum of first n natural numbers using recursion */

# include <stdio.h>

int sum(int n)
{
    if(n == 0 || n == 1)
        return n;
        
    return sum(n - 1) + n;
}

// testing
void main()
{
    printf(" %d ", sum(10));
}
