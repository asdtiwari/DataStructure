/* Array of Pointer */

# include <stdio.h>

void main ()
{
    // array of pointers to constant string
    char *ptr[4] = {"Akhilesh", "is", "introspecting", "itself"};
    
    for (int i = 0; i < 4; i++)
        printf("%s ", *(ptr + i));
    printf("\n");
}
