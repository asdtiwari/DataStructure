/* Initialise an array and display using pointer */

# include <stdio.h>

void main ()
{
    int array [5] = {1, 2, 3, 4, 5};
    
    int *ptr = &array[0];
    
    for (int i = 0; i < 5; i++)
        printf(" %d ", *(ptr + i));
    printf("\n");
}
